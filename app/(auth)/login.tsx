// Placeholder login screen; real form (RHF + Zod) arrives in Phase 2.
import { Text, View } from 'react-native';

export default function Login() {
  // Kept minimal so the route group boots during foundation setup.
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Angan — Login</Text>
    </View>
  );
}
