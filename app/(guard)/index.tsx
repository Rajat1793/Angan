// Guard gate placeholder until the live queue is built.
import { Text, View } from 'react-native';

export default function GuardGate() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Guard Gate</Text>
    </View>
  );
}
